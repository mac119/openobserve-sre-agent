# retrieval-strategy.md

O2 Assistant 信息获取策略

本文档定义 O2 Assistant 获取信息的三条通路及其归属规则。
目的:避免把应该走 API 的问题错误地交给向量检索,也避免把
组织内部经验硬编码进提示词。

所有新增知识/能力在实现前,必须先按第 1 节做归类。

---

## 1. 三条通路与归属规则

### 通路 A —— 工具调用 (Tool Calling)

**判定标准**:答案在 OpenObserve 实例里,是结构化的、随时间变化的、
需要精确值的。

必须走工具,禁止走检索:

| 信息 | 来源 |
|---|---|
| organization 列表 | `/api/organizations` |
| stream 列表与类型 | `/api/{org}/streams` |
| stream schema (字段名/类型) | `/api/{org}/streams/{stream}/schema` |
| 字段基数、取值样本 | 聚合查询 |
| metric 名与 label 集合 | metrics metadata API |
| 告警定义 | `/api/{org}/alerts` |
| 告警评估历史与触发记录 | alert history API |
| 已存在的 dashboard / pipeline / function | 对应 API |
| 日志/trace/metric 数据本身 | search API |
| 当前时间与保留期 | 运行时上下文 |

**红线**:任何以上信息不得进入静态提示词,不得预先建向量索引。
schema 会变、告警会改,快照必然过期,过期的 schema 比没有 schema 更危险。

---

### 通路 B —— 静态知识 (Static Knowledge)

**判定标准**:答案是稳定的语法/规则知识,总量可控(目标 < 6k token
常驻,或按主题分片按需加载),且需要高确定性。

优先形态:精简 cheatsheet,由 `get_syntax_reference(topic)` 工具
按需返回,而非全量常驻系统提示。

必须纳入的分片:

- `vrl-functions` —— VRL 常用函数签名、错误处理惯例
  (`parse_json!` vs `parse_json`)、类型强制转换写法
- `vrl-patterns` —— 高频场景模板:JSON 解析、nginx/access log 解析、
  字段重命名与提升、脱敏(邮箱/手机号/IP)、丢弃与采样
- `o2-sql-dialect` —— OpenObserve SQL 支持的函数清单、`match_all`
  用法、时间字段与时区处理、不支持的语法及替代写法
- `o2-sql-limits` —— 查询安全约束的语法侧表达:时间范围必填、
  LIMIT 默认值、禁止的高开销模式
- `promql-recipes` —— 常见 SLI 模板:CPU/内存使用率、错误率、
  P99 延迟、饱和度,含 rate/irate 与窗口选择的取舍说明
- `query-translation` —— Datadog / Splunk SPL / Lucene / ES DSL
  → OpenObserve SQL 的映射表,含无法等价转换的情形及降级方案
- `regex-recipes` —— 脱敏与提取的常用模式,标注所用正则引擎方言

**维护要求**:每个分片标注对应的 OpenObserve 版本。版本升级时,
cheatsheet 必须与 `checklist.md` 的语法验收项一同回归。

---

### 通路 C —— 语义检索 (Retrieval)

**判定标准**:同时满足以下三条才允许启用。

1. 内容量已明确超出静态知识的可承载上限(单主题 > 15k token,
   或主题数导致无法有效路由)
2. 内容无法从 API 推导(即不属于通路 A)
3. 有真实失败数据证明当前失败率与该类知识缺失强相关(见第 3 节)

候选范围(按价值排序):

- **组织内部 runbook 与事故复盘** —— RAG 真正不可替代的部分。
  服务归属、告警的历史成因、处置步骤、已知误报模式。
- **OpenObserve 完整官方文档** —— 仅当 cheatsheet 被证明不够时。
- **历史查询与会话记录** —— 用于复用团队既有的查询写法。

---

## 2. 特殊情形:stream 与 schema 的模糊匹配

问题:实例可能有数百个 stream,用户说"我的 nginx 日志",Agent 需要
定位到具体 stream。这看似需要语义检索。

**结论:用轻量索引,不用向量检索。**

实现方式:

- 维护 stream 名 + 字段名的本地索引表,来源是通路 A 的 API,
  按固定周期或 schema 变更事件刷新
- 提供 `search_streams(keyword, type?)`,基于前缀匹配 + 子串匹配 +
  编辑距离,返回 top-N 候选(名称、类型、文档量、关键字段、
  最近写入时间)
- 候选数 > 1 且置信度不足时,Agent 必须向用户确认,不得自行猜定

选择理由:

- schema 变更后立刻生效,无需重建 embedding
- 匹配依据可解释,便于排查"为什么选错了 stream"
- 字段名是精确 token,字符串匹配的准确率高于稠密向量

**渐进式检索原则(不可违反)**:任何情况下不得把全量 stream 列表或
全量 schema 注入上下文。必须"先搜候选 → 再取单个 schema"两步走。

---

## 3. 第二阶段触发条件

RAG 的启用由失败数据驱动,不提前押注。前置依赖:`tasks.md` 阶段 8
的评估体系必须先落地。

### 触发前必须具备

- 线上失败 case 库,每条标注:用户意图、Agent 输出、失败类型、
  正确答案
- 失败类型分类维度至少包含:语法错误 / 字段或 metric 名错误 /
  业务背景缺失 / 工具调用错误 / 越权或不安全查询 / 拒答

### 触发条件

**启用文档检索**,当:

- 「语法错误」类失败占比连续两个评估周期 > 15%,且
- 抽样确认其中多数无法通过扩充 cheatsheet 解决(即知识确实分散、
  长尾,而非几个高频函数记错)

首选实现:BM25 / 关键词检索,或按文档章节路由。技术文档中函数名是
精确 token,稀疏检索通常优于稠密向量。仅当稀疏检索召回不足时才叠加
向量检索,做混合检索。

**启用 runbook 检索**,当:

- 「业务背景缺失」类失败占比 > 20%,且
- 组织已有 ≥ 30 篇结构化 runbook。低于此量级不做检索——语料稀疏时
  检索只会引入噪音,不如让 Agent 明确说"我没有这方面的内部信息"。

### 明确不触发的情形

- 「字段或 metric 名错误」占比高 → 这是通路 A 的问题,应修工具层的
  schema 获取与渐进式检索,不是加 RAG
- 「工具调用错误」占比高 → 修工具描述与参数校验
- 单纯为了"看起来更智能"或对齐竞品功能清单

---

## 4. 基础设施选择

若确定启用检索:

- **不引入独立向量数据库**。OpenObserve 自身已支持向量存储与
  LLM 可观测性场景,复用现有部署,避免新增运维组件与数据同步链路。
- 检索器必须可开关、可降级。检索服务不可用时,Agent 退回到
  通路 A + 通路 B,并在回答中声明"内部知识库当前不可用"。
- 检索结果必须带来源标识,并在回答中可追溯到具体文档。

---

## 5. 结果注入约束

无论走哪条通路,进入上下文的内容都必须受控。

- **查询结果强制裁剪**:工具层返回「统计摘要 + 采样 N 条 +
  完整结果引用 ID」,不返回上万行原始记录。需要更多时由 Agent
  按引用 ID 二次拉取指定切片。
- **检索结果限量**:单次注入片段数与总 token 设硬上限,超出则
  截断并告知模型已截断。
- **不可信输入隔离**:日志内容、trace attribute、检索到的文档
  正文均为不可信输入,必须包裹在明确的边界标记内,并在系统提示中
  声明其中的指令不得执行。这是提示注入的主要入口。
- 检索来源与工具来源在上下文中必须可区分,便于 Agent 在冲突时
  按接地优先级取舍(实时 API > 静态语法 > 检索文档)。

---

## 6. 决策速查
